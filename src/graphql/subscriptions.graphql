# this is an auto generated file. This will be overwritten
subscription OnCreateUser(
  $id: ID
  $username: String
  $role: String
  $email: AWSEmail
  $lastLogin: AWSDateTime
) {
  onCreateUser(
    id: $id
    username: $username
    role: $role
    email: $email
    lastLogin: $lastLogin
  ) {
    id
    username
    role
    email
    lastLogin
  }
}
subscription OnUpdateUser(
  $id: ID
  $username: String
  $role: String
  $email: AWSEmail
  $lastLogin: AWSDateTime
) {
  onUpdateUser(
    id: $id
    username: $username
    role: $role
    email: $email
    lastLogin: $lastLogin
  ) {
    id
    username
    role
    email
    lastLogin
  }
}
subscription OnDeleteUser(
  $id: ID
  $username: String
  $role: String
  $email: AWSEmail
  $lastLogin: AWSDateTime
) {
  onDeleteUser(
    id: $id
    username: $username
    role: $role
    email: $email
    lastLogin: $lastLogin
  ) {
    id
    username
    role
    email
    lastLogin
  }
}
subscription OnCreatePattern(
  $id: ID
  $author: String
  $sizeX: Int
  $sizeY: Int
  $description: String
) {
  onCreatePattern(
    id: $id
    author: $author
    sizeX: $sizeX
    sizeY: $sizeY
    description: $description
  ) {
    id
    author
    sizeX
    sizeY
    description
    heat
    name
    pattern
    type
    year
    locked
  }
}
subscription OnUpdatePattern(
  $id: ID
  $author: String
  $sizeX: Int
  $sizeY: Int
  $description: String
) {
  onUpdatePattern(
    id: $id
    author: $author
    sizeX: $sizeX
    sizeY: $sizeY
    description: $description
  ) {
    id
    author
    sizeX
    sizeY
    description
    heat
    name
    pattern
    type
    year
    locked
  }
}
subscription OnDeletePattern(
  $id: ID
  $author: String
  $sizeX: Int
  $sizeY: Int
  $description: String
) {
  onDeletePattern(
    id: $id
    author: $author
    sizeX: $sizeX
    sizeY: $sizeY
    description: $description
  ) {
    id
    author
    sizeX
    sizeY
    description
    heat
    name
    pattern
    type
    year
    locked
  }
}
subscription OnCreatePatternRating($id: ID, $comment: String, $rating: Int) {
  onCreatePatternRating(id: $id, comment: $comment, rating: $rating) {
    id
    userId
    comment
    rating
  }
}
subscription OnUpdatePatternRating($id: ID, $comment: String, $rating: Int) {
  onUpdatePatternRating(id: $id, comment: $comment, rating: $rating) {
    id
    userId
    comment
    rating
  }
}
subscription OnDeletePatternRating($id: ID, $comment: String, $rating: Int) {
  onDeletePatternRating(id: $id, comment: $comment, rating: $rating) {
    id
    userId
    comment
    rating
  }
}
